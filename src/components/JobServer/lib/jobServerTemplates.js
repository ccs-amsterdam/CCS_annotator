// make some basic templates for jobservers
// - given an array of units, a codebook, and rules (seek_forwards, etc.). Return annotations via callback
// - for async tasks, basically the same, but instead of array of units need getUnit function
//     - use i argument to controll flow
