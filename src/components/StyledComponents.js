import styled from "styled-components";
import { Segment } from "semantic-ui-react";

// Not yet implemented, but keeping as a placeholder because
// this will probably be the nicest way to dry things at some point

export const StyledSegment = styled(Segment)({
  border: "0",
});
